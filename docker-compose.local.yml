services:
  db:
    image: postgres:16
    container_name: sendit-db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata-local:/var/lib/postgresql/data

volumes:
  pgdata-local:
